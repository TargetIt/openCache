# gpgpusim_cache 遗留问题跟踪

更新时间：2026-06-01

本文档作为固定入口维护 `gpgpusim_cache` 的遗留问题、质量缺口和后续验证工作。规则：

1. 已解决的问题用 `[x]` 标记，并保留解决说明。
2. 未解决的问题用 `[ ]` 标记，不删除，直到完成并刷新验证结果。
3. 每次新增需求、测试、修复或覆盖率变化后，同步更新本文档。
4. 历史说明可参考 `遗留问题_20260528_230000.md`，本文档只维护当前跟踪状态。

## 当前质量判断

当前覆盖率基线来自 `./coverage.sh`：

| 指标 | 当前覆盖率 | 水平判断 |
|------|------------|----------|
| Region | 53.15% | 偏低，复杂条件块覆盖仍不足 |
| Function | 69.62% | 中等，仍有函数未进入 |
| Line | 67.55% | 中等，可作为回归起点 |
| Branch | 57.92% | 偏低，异常路径和策略分支仍需加强 |

结论：当前验证体系已经可以作为默认回归门槛的起点，但还不能视为零缺陷验证收敛。下一阶段应优先提升 branch coverage 和异常路径覆盖。

## 已解决

- [x] 建立 `gpgpusim_cache` 验证方案、feature 矩阵、testcase 矩阵和覆盖率基线。
  - 位置：`doc/verification/verification_plan.md`
  - 位置：`doc/verification/feature_matrix.md`
  - 位置：`doc/verification/testcase_matrix.md`
  - 位置：`doc/verification/coverage_baseline.md`

- [x] 修复原 unit test 断言失败仍可能显示通过的风险。
  - 位置：`test/test_main.cc`
  - 结果：`./run.sh` 可正确以非零状态报告失败。

- [x] 建立一键回归脚本。
  - 位置：`run.sh`
  - 当前结果：unit `13/13`、scenario `86/86 checks`、whitebox `17/17`、death `6/6` 全通过。

- [x] 建立覆盖率脚本。
  - 位置：`coverage.sh`
  - 当前结果：Region `53.15%`、Function `69.62%`、Line `67.55%`、Branch `57.92%`。

- [x] 补充 set-index function 的基础范围检查和选定地址 golden 值。
  - 覆盖：LINEAR、XOR、IPOLY、FERMI、CUSTOM。
  - 位置：`test/test_cache_whitebox.cc`

- [x] 扩大 set-index/hash golden 地址集。
  - 覆盖：9 个边界/高位地址，LINEAR、XOR、standalone IPOLY、CUSTOM、FERMI 32-set、FERMI 64-set。
  - 位置：`test/test_cache_whitebox.cc`

- [x] 补充 all-reserved line allocation fail 用例。
  - 覆盖：`RESERVATION_FAIL` 和 `res_fails` 统计。
  - 位置：`test/test_cache_whitebox.cc`

- [x] 补充 lazy fetch partial write/readable 行为。
  - 覆盖：partial write 后 read 触发下级读，full-sector write 后 read 命中。
  - 位置：`test/test_cache_whitebox.cc`

- [x] 补充 `LOCAL_WB_GLOBAL_WT` 行为。
  - 覆盖：global write hit write-through，local write hit write-back。
  - 位置：`test/test_cache_whitebox.cc`

- [x] 补充 `FETCH_ON_WRITE` 行为。
  - 覆盖：full-line write 不 fetch，partial write 触发 write-allocate。
  - 位置：`test/test_cache_whitebox.cc`

- [x] 补充 flush/invalidate 语义断言。
  - 覆盖：flush 后 dirty line miss、clean line hit；invalidate 后多 line miss。
  - 位置：`test/test_cache_whitebox.cc`

- [x] 修复 `DataStore` 同 block 短写截断旧数据的问题。
  - 位置：`gpgpu_cache/data_store.h`
  - 覆盖：partial write preserve。

- [x] 建立 death test 框架，覆盖非法配置和 assert 路径。
  - 位置：`test/test_death.cc`
  - 覆盖：parse error、sector line size、writeback/on-fill、lazy/on-fill、data port width、Fermi nset。
  - 说明：death test 通过子进程隔离 abort，不中断默认回归。

- [x] 补齐 `cache_stats` fail reason 精确查询。
  - 位置：`gpgpu_cache/gpu_cache_ref.h`
  - 位置：`gpgpu_cache/gpu_cache_ref.cc`
  - 覆盖：`LINE_ALLOC_FAIL`、`MISS_QUEUE_FULL`、`MSHR_ENRTY_FAIL`、`MSHR_MERGE_ENRTY_FAIL`、`MSHR_RW_PENDING`。

- [x] 扩展 sector dirty mask/writeback 组合。
  - 位置：`test/test_cache_whitebox.cc`
  - 覆盖：多 dirty sector 驱逐时 writeback modified size 和 sector mask。

- [x] 扩展 texture result FIFO backpressure。
  - 位置：`test/test_cache_whitebox.cc`
  - 覆盖：result FIFO 满时后续 ready 被阻塞，pop 后继续推进。

- [x] 扩展 texture `SECTOR_TEX_FIFO` pending_read 和多请求返回顺序。
  - 位置：`test/test_cache_whitebox.cc`
  - 覆盖：4 个 sector response 全部返回后才 ready；两个 texture miss 乱序 fill 时仍按 ROB 顺序返回。

- [x] 处理 `llvm-cov` profile mismatch 警告。
  - 位置：`coverage.sh`
  - 说明：`llvm-cov` 合并多个测试 binary 的 profile 时仍会产生 mismatched function data 诊断；脚本已将原始诊断保存到 `coverage/coverage-warnings.txt`，并在 summary 中输出受控说明，避免误判为覆盖率脚本失败。

- [x] 扩展 data cache `SECTOR_ASSOC` pending_read。
  - 位置：`test/test_cache_whitebox.cc`
  - 覆盖：前三个 sector child response 不 ready；第四个 response 到齐后恢复 original request 的地址和 data size，并通过 MSHR ready 返回。

- [x] 增加多 seed randomized/property 基础回归。
  - 位置：`test/test_cache_whitebox.cc`
  - 覆盖：5 个固定 seed、每 seed 512 次 read-only 随机访问、重复运行差分一致性、access/miss/reservation fail 不变量。

## 未解决

- [ ] 覆盖率仍未达到高质量收敛水平。
  - 当前 Line `67.55%`，Branch `57.92%`。
  - 目标：后续至少把 branch coverage 提升到可接受门槛，并解释无法覆盖的代码。

- [ ] MSHR 长 trace 和复杂 differential/property 测试仍不足。
  - 当前已有 fixed seed property trace 和 5-seed read-only differential trace。
  - 后续需要更长 trace、读写混合、与参考模型对照。

## 下一步优先级

1. 继续提升 branch coverage，并把每次提升同步到本文档和 `doc/verification/coverage_baseline.md`。
2. 扩展更长、更复杂读写混合的 differential/property trace。
